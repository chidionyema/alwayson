module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Substitute "Inter" with your chosen font
      },
      colors: {
        gray: {
          50: '#f7f7f7', // Light background
          900: '#1a1a1a', // Dark text
        },
        primary: '#007aff', // Example primary color (Apple blue)
        secondary: '#5e5ce6', // Secondary color
        // Add additional colors as needed
      },
    },
  },

  plugins: [],
};
